{
  "name": "org-ietf-sdp",
  "private": true,
  "description": "SDP (RFC 4566) session description grammar in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljk"
  }
}
